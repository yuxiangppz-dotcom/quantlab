# 四处衔接与恢复修复后的自审

起始 HEAD：`3230e0079244fcd23c0f700272223709f14cf49e`。直接单 Agent 委托；PR #189，
不自动合并。本次所有市场输入为合成测试，未调用真实供应商、写真实 Canonical、改个人账户或下单。

## 发现、修复及验证

| 问题 | 修复 | 主要验证 |
|---|---|---|
| 全市场开户报价与研究子股票池不等导致首日失败 | 空仓首日采用当天报价范围；共同证券价格仍须一致 | 子集首日生成决策，冲突价格仍拒绝 |
| R 复牌被当成 S 停牌 | 按类型和收盘时点解读日内区间；矛盾/未知保持未知 | 复牌、全日/日内停牌、缺行情、冲突及错误时间 |
| 旧代码缺当前主表记录导致特征 KeyError | 复用已有上市/代码变更生效/终止日期规则；绑定来源文件 | 旧代码在生效日前有效，新代码在生效日后有效，不跨代码拼接，未知身份拒绝 |
| 模型暂时不可用会冻结当天受阻状态 | 不可改写账本 + 截止前追加决策尝试 | 模型恢复、暂停解除、已结算日重试、超时、中断、重复、次日结算和决策指纹 |
| 自审额外发现：昨日待买证券退出今日股票池会缺执行证据 | 当日市场范围补上昨日冻结订单证券 | 空持仓但有待买订单时仍保留该证券 |

重试失败记录与成功计划均保留。第一份完整计划一旦发布不再替换；发布时越过截止时间则无前向资格。
次日账户绑定实际采用的决策和发布时间指纹，报告、监控跟随同一个决策解析入口。
已封存输入变化仍拒绝覆盖，不以重试之名修订历史证据或放宽费用、可交易性和时序约束。

## 整体评价

当前可以评价为：具备单一主入口、可复现训练/回放、连续模拟账本及明确恢复规则的个人日频研究框架。
不能评价为：已通过真实来源验收、真实长期运行验收、或者已经证明策略有效的系统。

| 验收层 | 已实现能力 | 仍缺的证据/边界 |
|---|---|---|
| 框架完整性 | 数据接入→共同特征→成熟标签→滚动训练→组合/订单→账户→报告 | 本地四份来源输入仍须绑定，不自动制造历史事实 |
| 时序可信 | 标签成熟、独立验证、实际发布时间、晚到不补单 | 历史可得时间、行业/资格来源还未实证核验 |
| 会计可信 | 分/股账本、费用、拒单、应收与到账分离 | 真实费用、复杂公司行动、特殊退市和跨代码持仓变更需证据 |
| 故障恢复 | 重复执行防重记、账本提交后决策重试、截止保护、来源/决策指纹 | 本机断网/断电/磁盘不足和备份恢复演练 |
| 持续运行 | 固定账户/模型柜，按月独立训练目录，暂停/过期/滞后状态 | 本地定时器、告警和至少 20 个交易日实际观察 |
| 策略有效 | 信号诊断、净值、费用、换手、基准与风险报告 | 真实样本外结果、成本与资本情景；没有盈利结论 |

本次不是增加模型复杂度或优化历史收益，而是修复已确认的框架接线问题。代码测试只认证覆盖的行为。
不进行无证据的自动证券代码持仓转换，不把原始日线/复权价和真实成交混用。

## 迁移与本地交接

新特征合同记录带日期的身份规则，须在新 workspace 重建 bundle、训练并注册；旧封存实验保留。
旧合同模拟账户不通过编辑 service.json 迁移，本次验收建立隔离的新合同账户，不重写旧收益曲线。
同一合同下日常重训仍保留稳定账户。详见 `ml_v2_local_completion_prompt_zh.md`。

## 本轮检查记录

- 专项 63 项通过，含 pipeline / ML service / reliability / operations，启用真实 Qlib 运行时。
- 全仓（启用可选研究运行时、未过滤）：3,614 项通过，1 项失败。失败为此前已确认的云端
  `tests/daily/test_local_ui.py::test_real_local_server_start_reuse_and_graceful_stop`：子进程 PID
  归属核验受当前命名空间限制而超时；本轮没有修改 UI 进程保护。4 条 Qlib 运行时既有警告。
- 执行方式：`UV_PROJECT_ENVIRONMENT=<锁定环境> QUANTLAB_TEST_OPTIONAL_RESEARCH=1
  uv run --frozen --no-sync pytest -q`；复用已安装的锁定环境，未临时升级依赖。
- `uv run --frozen --no-sync ruff check .` 与 `git diff --check` 通过。
- 相比起始提交新增 26 个测试用例。所有测试只用临时合成市场资料，不代表真实数据验收。
- GitHub CI 继续执行未过滤全仓及可选研究测试；以本轮修复提交的 Actions 结果为准。

---

# 整体整合的自审记录

起始 HEAD：`8a7a145ea75a015e77151cfafe51dae46180cddb`。本轮仍在 PR #189 的独立分支，
未合并主分支。按直接单 Agent 委托进行实现、测试和自审，未并行修改同一工作区。

## 审查结论与已修复问题

| 发现 | 处理 |
|---|---|
| ML 入口之前的数据下载/特征准备仍依赖手工拼接 | 新 pipeline 配置贯穿 TuShare、Canonical、Alpha158、训练、交易证据和每日账户 |
| 下载无统一原始来源记录与有限重试 | ObservedClient 记录实际响应与获取时间、节流、有限重试、脱敏错误；日频响应上限保护 |
| 文件存在被当作同步完成，半写入难以恢复 | 全日七类数据先验证并暂存，再提交；完成回执绑定哈希，待提交事务可恢复，旧分区需明确接管 |
| SSE/SZSE 缺日/冲突可能被合并隐藏 | 新同步要求请求区间逐日一致且完整；不从缺行推断休市 |
| 增量更新与历史更新对上市前占位行处理不一致 | 复用同一已知上市日过滤函数，不以缺 pre_close 擅自过滤 |
| 历史/每日特征容易分叉，多年处理可能超内存 | 固定 native Alpha158 单一计算函数、63 日分批和预算限制，真实 Qlib 一致性测试 |
| 时间戳单位和窗口来源传播容易出错 | 按真实带时区时间取窗口最大值；不得把 pandas 微秒整数误当纳秒；收盘数据不得早于收盘可知 |
| 退出股票池的标签端点可能丢失 | 价格保留整个已声明研究范围内证券，特征资格仍按当时 context |
| 固定 16:00 与数据 15:00–17:00 更新冲突 | decision_hour 贯穿特征、模型、账户、发布和风格报告；新配置 18:00，旧省略值仍 16:00 |
| 未封存发布回执可能被单独改截止时间 | 回执必须与已封存预测/账户中的截止策略一致 |
| 统一研究入口把打分首日当成交首日 | 回放自动从首个打分日的下一交易日开始 |
| 每月重训 workspace 与持续账户混在一起 | account/registry 独立稳定路径，研究 workspace 分期；输入变化不覆盖既有产物 |
| 本机配置会导致工作树不干净、阻断研究 | 明确忽略 project.local.json / project.*.local.json；仍保留干净代码运行要求 |
| 金额单位倍乘可能制造浮点尾差 | 供应商十进制值先缩放再进入既有浮点存储；账本仍要求可精确表示的整数分/股 |

## 验证范围

- 全仓回归：3,588 项通过，1 项云端 UI 子进程 PID 命名空间检查排除。该项原保护没有削弱，
  GitHub Linux CI 继续运行未过滤的全仓测试。
- 最后一轮受影响模块：70 项通过，含真实 Qlib、来源到模型/回放/报告/连续账户、事务中断恢复、
  来源变更拒绝、批量/单日一致性、晚到输入、截止策略篡改、TuShare 及原增量更新回归。
- Ruff 与 diff whitespace 检查通过。CI 的可选研究任务已加入 pipeline 测试。
- 所有源数据均为测试内临时生成；没有调用真实供应商、写真实 Canonical、修改个人实账或下单。

## 仍然不能声称的事情

尚未认证本地来源时点、历史行业/资格、费用、公司行动或实际可成交性；没有真实策略收益结论；
没有在本机安装定时器或完成连续前向观察。标准整合范围为 A 股日频研究与模拟，特殊公司行动、
退市清算等依然必须有证据才扩展。生产可靠性不能由合成测试替代。

下一步使用 `ml_v2_local_completion_prompt_zh.md` 做数据和运行验收，而不是重复实现框架。
旧冻结实验及其共享底层工具保留兼容；停止把它们当作新日常入口，没有为目录整洁删除来源能力。
