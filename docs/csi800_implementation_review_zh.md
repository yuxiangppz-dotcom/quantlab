# CSI800 整体改造自审记录

起始HEAD：`d15357e0acfd2bc4ff879c88f3bd7a2538b44058`。本记录随改造提交保存；最终HEAD与CI结果见PR #189。
范围是无真实行情云端能够实现和验证的框架工作，未进行实际供应商请求、真实Canonical接管、券商下单或真实账户修改。
测试中的行情、股票名单、费用和账户均为临时目录里的合成事实，不是市场业绩。

## 已核验的修正

| 发现/缺口 | 修正及覆盖 |
|---|---|
| ST/停牌响应触顶仍可能被封存 | 共享行数/日期校验，旧回执再核对；主表/ST供应商入口补触顶拒绝 |
| 换基准后复用的旧回执没有所需指数 | 同步明确拒绝缺000906的旧完成日，要求新数据版本 |
| 受阻每日决策仍返回0 | 暂停、迟到、模型阻断均非零；账本已结算与正常前向决策分开 |
| 股票池依赖外部单一eligible | 历史CSI800生成器，成分/上市年龄/ST/流动性/来源与原因码，流式写出 |
| 研究资格与持仓处置混用 | eligible/can_open/must_exit/soft_exit贯穿特征、回放、每日计划 |
| 调出/退市历史证券要求每天有新行情 | 历史资格仍留存；不制造价格/执行上下文，真实持仓缺证据仍由账本阻断 |
| 过期行业会阻断长期调出且未持有证券 | 非成员可明确保留行业未知；若真实持有，组合仍拒绝未知风险 |
| 普通停牌缺估值 | 声明后可限龄沿用已知停牌前原始收盘；不变成成交价，跨公司行动拒绝 |
| study与压力/风险报告未进主入口 | 主线绑定study/冻结策略，独立资金与5/10/20bp情景、年度/风格/预算对照 |
| 保存模型等同于可启用 | 候选、证据绑定模拟准入、真实时刻启用分开；账户也核验release |
| 首次研究后缺更新入口 | 同冻结方法refresh，验证成熟标签与rank IC，不自动activate |
| refresh嵌套构建重复拿同一路径锁 | 合成端到端测试复现，改用独立编排锁；完整更新失败路径通过 |
| 模型失效时硬风险也没有计划 | V2允许及时已知风险的仅卖出计划，仍报模型受阻，不伪装正常信号 |
| 漂移有记录但成熟标签未回收 | 只评价账户实际采用的按时信号，处理重试版本、未成熟/缺端点、IC/覆盖/漂移告警 |
| 运维依赖散落文档 | health、调度脚本、隔离backup/verify-backup、revision-plan、统一项目路径UI |

## 验证记录

- 未过滤全仓库、可选研究运行时：3635通过、1失败、4警告；失败为既有
  `tests/daily/test_local_ui.py::test_real_local_server_start_reuse_and_graceful_stop`。
  当前云端子进程PID与可见`/proc`不一致，健康接口与进程归属核验不能同时满足；保护逻辑没有修改。
- 修正后的全仓库回归：3636通过、1项上述环境检查排除、4条既有研究警告。
- 相关专项曾有116项通过；随后扩展源数据→训练→多资金/滑点回放→候选更新→账户的集成用例通过。
- 最后行业缺失范围修正另跑历史成分/未知证据定向用例；Ruff及`git diff --check`通过。
- GitHub CI保留未过滤默认全仓库和可选研究测试；最终以PR最新提交的实际结果为准。
- 资源侧采用资格逐日Parquet写入、已有特征分批及训练矩阵预算；没有真实800股票多年训练RSS/耗时实测，留作本地必验项。

## 不能据此宣布完成的事项

来源格式、哈希和时间检查不等于已独立认证历史PIT事实。中证800公告/生效区间、行业历史、ST覆盖、
来源修订、费用和公司行动尚待真实接入。配股、持有期差异税、合并换股、退市结算等特殊会计仍可能需要专项实现。
同池可实施基线、总收益基准、因子消融与基本面增量需本地补充和实验。
滑点/容量目前是明确情景，不是校准后的市场冲击模型。备份自检不是恢复演练；调度脚本不是已安装运行的服务。
没有宣称有效alpha、保证收益、实盘权限或“所有问题已清零”。

建议冻结当前研究语义，按 `docs/ml_v2_local_completion_prompt_zh.md` 进行真实数据和持续模拟验收；
发现真实源或会计反例就补有证据的适配和测试，保留旧数据/实验/账户，不靠调门槛或删样本过关。
