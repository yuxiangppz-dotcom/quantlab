# 固定 S4 首个交易日的输入连接

已把保存的 S4-A 信号、未复权行情、历史涨跌停与前 20 日成交额连接成
逐股研究输入对象。这部分没有选股、模拟订单、计算净值或重新运行旧信号。
固定信号日为 2021-12-31，拟执行日为 2022-01-04，下一交易日为 1 月 5 日；
256 只原始研究股票全部保留，不能按当天是否好交易更换研究范围。

本次一次材料转换读取 24 个正式数据分区和一个已封存信号表，均先绑定文件
指纹。前 20 个交易日为 2021-12-06 至 12 月 31 日；没有拿执行日的成交额
补决策日的容量，也没有把已经转换为元、股的正式数据再乘一次千元、手单位。

| 逐股输入 | 本次结果 |
|---|---:|
| 原有 S4-A 信号可直接复制 | 250 / 256 |
| 原始 OHLC 可按整数分表达且关系正确 | 253 / 256 |
| 前 20 日成交额都满足严格整数分表达 | 169 / 256 |
| 当天行情与已知涨跌停边界一致 | 253 / 256 |
| 有 ST 异常记录的股票 | 11 |
| 有停复牌记录的股票 | 3 |

169 不等于其余 87 只完全没有成交额资料。严格检查中有 142 个证券/日期问题：
56 个是缺行，86 个是现存数值含不足一分的小数。例如一笔正式成交额为
`131464165.00000001` 元。这种尾数与浮点单位转换误差相符，但本次固定合同
没有允许自动舍入，因此仍标未知，并未篡改原数据或放宽已封存结果。
后续容量计算可以单独检验“对非负成交额向下取整得到保守容量下界”，而不是
把这类微小尾数误报成大面积数据缺失；价格、成交现金和缺行不能跟着猜测。

这里的异常表没有给出完整的“正常可交易”证明。输入对象的市场可交易状态、
公司行为处理、收盘撮合规则资格、参与率和完整费用情景仍明确为空。
此前规则 v3 的连续竞价数量范围、公司行为的字段齐全，也没有被直接提升为
本次完整成交/现金资格。因此现在仍不能从这些输入声称扣费收益已可信。

新增 46 项测试通过，全套 2494 项通过、10 项可选跳过，56 条既有 NumPy
警告保留；Ruff 与差异检查通过。首次检查修正了测试装饰器冒号和一处长行。
单次转换约 0.98 秒、峰值约 204 MiB，输出约 336 KiB；另一程序用分数算术
逐行复核全部 256 条及汇总，通过。两程序由同一代理编写，不称为独立人员审核。

起始主线 `d4f9f536cf60ea976a78dfb1562ed21de7a2efa1`，任务卡 `4f4dbd6`，
先推送的实现/实际转换源码 `ed4e047a983b805abd383ceaafea697d1f994986`。
报告指纹 `e8ea83c4503107b6a14de3e6b7ad30d7ad80fc4578c541d7bdfdf794b8df176d`；
复核指纹 `fb90eab29dddd9b5f37d4ed604ff22357b4ed07bf8fb67e0b20f1f31342cde8e`。
目录为 `data/products/research_program/launch_20260912/stock_replay_inputs`，
实际转换和证明均封存，不重复运行；提交和精确主线 CI 在独立收尾收据保存。

本批没有数据商请求、经济路径、模型训练、正式数据/账户改写或策略晋升。
下一步把仍未解决的历史资格单独列出，推进不依赖它们的、及时登记的 S4
前瞻信号观察；这也不能代替扣费验证，不能倒填已过去交易日的前瞻记录。
