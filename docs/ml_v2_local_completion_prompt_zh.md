# 本地 Codex 提示词：CSI800完整验收与剩余接入

复制以下正文。任务同时包含真实数据验收及仍需源数据才能设计/验证的扩展；不要将扩展误报为已经完成。

---

请继续我的QuantLab，接通并验收历史中证800的研究与持续模拟主线，持续自审、修复、测试、提交推送。
不要只给计划，不围绕旧-98%曲线调参，不再并入多套完整框架。

先读AGENTS.md、README、docs/csi800_project_zh.md、config/project.example.json、
config/strategy.csi800.json、config/ml_daily_v2.json，以及pipeline/、research/ml/。
核实PR #189、codex/ml-framework-v2-20260916最新状态；未合并从最新PR head隔离工作树开始，
已合并从包含这些修改的主分支开始。保护本地工作，确认没有别的写入者，不强推。

环境：Windows/WSL2、12600KF、16GB、研究资金20万元、TuShare约5000积分、有2010–2026行情。
股票池固定为当时的中证800，不能用今日800只回填，不偷偷换成全市场或赢家子集。
ST不研究和新买；调出、停牌、卖不掉的已有持仓继续记账。

授权只读盘点、修复代码、生成隔离研究产物、训练/回放/模拟、测试、提交推送。
供应商请求、Canonical接管/写入、操作系统定时器部署按我在本地会话明确授权的范围执行。
确需补授权时先完成可审查的配置、请求范围/数量和磁盘影响，说明精确缺项。
不下真实订单、不改personal真实账户、不删旧证据、不提交token/行情/模型/机器配置。

1. 锁定环境：uv sync --frozen --extra research --extra qlib。盘点价格/复权/daily_basic、
   日历/证券身份、指数、ST/停复牌/涨跌停、行业、公司行动的覆盖、单位、重复、缺失与可得时点。
   新版本数据、workspace、account、registry与旧实验隔离。旧封存index没有000906时，不能改旧回执；
   在新数据命名空间复用核验后的行情等分区，为新版本补指数并生成新回执。
   或实现有测试的补充指数适配。复用数据不得倒填历史观察/发布时间。

2. 建立历史中证800的真实证据。可用sync-index-observations --execute按月获取观察资料，
   再结合官方定期/临时调样公告生成完整members、生效区间、known_at、source_id、revision_id。
   月度weight不能倒填整月，也不能证明公告日。抽样核对调样附近、退市、调入调出。
   官方若真实出现非800个成分，保留依据、增加明确例外协议及测试，不能随意放宽。

3. 生成主线需要的industry intervals、event_coverage、source_availability.parquet，
   以及execution_policy、corporate_actions。读手册中的精确schema；写复用转换脚本和来源receipt，
   不手工拼数百万行，不用今日行业回填历史，不把空ST响应一律当“无ST”，不伪造complete/known_at。
   历史时点无法证实就如实记录研究局限与阻断。补前向资料滚动更新，保留每次事实版本和真实到达时刻。
   交易权限以我的券商证据确定，科创板/创业板属于研究池不等于有真实交易权限。

4. 按日期核对真实佣金/最低费/税费、各板块数量规则、成交参与率、分红净额/除权/到账日及送股锁定。
   普通停牌last_raw_close估值政策核验后再启用；跨除权、长停、退市不能直接沿用旧价。
   实际发现配股、持有期差异税、合并换股、零碎股、退市结算等未支持事件时，
   补有来源的专项会计适配及测试；无法完成则列出日期/证券/原因，不删事件、不跳过亏损日期。

5. 跑plan→sync→universe→init-study→build→train --resume→replay --resume→report→stress --resume。
   sync须已有授权。先短区间对齐与资源验收，再完整区间；记录RSS、磁盘、耗时。
   16GB不足先改批次/复制/线程，保持中证800定义，不挑股票或缩窗掩盖结果。
   核验成熟标签、训练purge、验证早停、下一收盘口径、现金/股数/T+1/涨跌停/最低佣金和卖出受阻。
   挑真实调样、分红、停牌日期独立手算对账，不能仅靠同一引擎自证。

6. 过去看过2023–2026，不能把2026再宣称全新holdout。保持试验台账，真正新证据来自之后前向运行。
   在相同开发协议内做有限的H=5/10/20、窗口和固定seed对照；不根据测试期选最佳。
   Alpha158按经济类别做冗余与消融；基本面先少量PIT可靠因子，再证明增量。
   补同历史池、同可交易规则/成本的等权或简单因子基线；有可靠总收益指数源再接入。
   现有000906价格指数与含股息组合口径不同，不能将全部差额称为alpha。
   核验行业/规模/波动/beta暴露；额外中性化或优化器须在同协议下有稳定净增量才保留。

7. register只是候选，release只是模拟观察准入，不是收益认证。失败保留候选和原因，不能降门槛造通过。
   成功后明确activate、记录真实时刻。历史模型超过45日，用refresh --as-of 日期 --parent-model-id 已准入ID --resume
   产生同冻结方法的新候选。验证IC、成熟标签、契约和来源通过再明确activate；更新失败保持旧启用记录。
   参数/策略/代码/运行时变化须重新研究准入，不能借refresh绕过成本验证。

8. 初始化隔离paper账户，每日运行daily、health、monitor。审查scripts/quantlab_daily_job.py，
   上海17:20左右启动、18:00冻结；定时器安装前确认授权、时区、WSL休眠、日志与失败通知。
   演练断网、迟到、重复启动、漏日、模型过期、同日阻断后重试。漏日顺序补账，不补造订单。
   核验模型失效时仅卖出的硬风险计划及非零状态。至少20个真实交易日验运行，然后长期积累成熟标签；
   20日不能证明alpha。

9. 核对空间，做backup、verify-backup，隔离环境真实恢复一次；验证账户链、来源/模型哈希、
   现金/股数/应收、下一交易日连续性。绝对路径绑定须保留或显式迁移并重验。
   修订数据用revision-plan及新数据版本；真实账本纠正须独立对账，不倒改过去美化结果。
   QUANTLAB_PROJECT指向同一配置，核对UI账户、状态、失败原因。

10. 自己review再修，运行全仓库测试、可选research/Qlib测试、Ruff和差异检查，提交推送并检查CI。
    云端有过一次UI进程命名空间检查失败，本地必须复验，不照抄跳过，不删除进程归属保护。
    最后给起止HEAD、修改清单、真实覆盖表、测试/实验结果、资源实测、事件支持范围、
    剩余缺项与阻断原因；说明各命令动作、每天先后顺序及哪些旧入口只保留历史复现。
    无真实证据不要宣布“可实盘”“有效alpha”或“全部问题清零”。
