# 固定股票样本的31只分红原始资料

已完成31/31次固定请求，全部返回非空结果，共1959条原始记录，未重试。
本次只查询原256股样本中没有出现在旧批次的31只股票；没有重下其余225只。
两批合计覆盖256只股票的原始响应、15190条原始行，**不等于已经证明完整分红历史，
也没有据此生成分红现金或策略收益**。

新取得的1959行中，152行在2020—2024有登记、除权、派息或红股上市日期。其他年份、
预案、终止状态和空日期均保留，没有为了回测窗口删除。存在1条精确重复记录，亦原样
保留。这一小批里已标记实施、税前现金为正却缺有效派息日的行数为0；不能将这个结果
推广到旧225只股票或理解为事件版本、付款和税款已经全部验入。

28只股票具有上述窗口内的事件日期；其余3只的返回记录没有这类日期，不能据此证明
当时绝无公司行为。按原有候选记录键还有8组不同内容需要对照，原始字节另行复核后
保留了冲突清单；这不是已确认的8次分红事件或8次错误。全历史原始行包括542行实施、
913行股东大会通过、491行预案、12行预披露和1行停止实施，不能把这些阶段相加计现金。

沿用16个官方字段，包括非默认的基准日和基准股本。股息和送转比例以每股为单位，
基准股本单位为万股；`cash_div`税后字段不能直接当成用户最终承担的股息税结果。
依据为[Tushare分红送股文档](https://tushare.pro/document/2?doc_id=103)。
旧解析器的2023—2026相关日期统计没有被改名当成2020—2024，本批单独计算并核对了
新的期间统计；原始各版本未被覆盖、合并或倒填。

实际下载18.46秒、峰值约140.9MiB，保存原始响应249584字节，原始运行输出约402KiB。
独立程序核对31条请求的参数、原始字节哈希、字段和日期、数值类型、行数、重复、
相关日期和费用/历史资格标志。它不调用下载器或解析器来重复形成同一份证明；这是
同一代理用另一程序自审，不冒充独立审核人员。

数据请求前已提交推送代码：`6fb7eb476653a8f5d1fff5035f5d4ef0c6c6c0ab`。
原始报告：`ec618b947719dd8aa86c0ca384f39ddda5b782b28e6f4959ba3b7eb445ef30db`。
复核报告：`3c6c89e4563864aa51a555b211cb4b701c31086b9a63efd9f6bc82b1aaff487e`。
证据目录：`data/products/research_program/launch_20260912/dividend_gap_intake`。
19项针对测试、全套2259项测试通过；10项本地可选运行时跳过、56条既有NumPy警告，
Ruff和差异检查通过。初次针对测试命令误写了旧用例路径，未执行测试或请求；纠正后
针对测试及全套均通过。实际下载无传输、权限、截断或格式失败。

本部分仅新增31次原始数据请求，新计划累计353次，仍零重试；3个S4规则、4个资金流
身份、2个Alpha公式、0条经济路径、0次模型拟合、0次晋升保持不变。没有修改正式数据，
没有读取正式行情作新回测，也没有创建真实委托、成交或账户流水。

下一步复用现有`dividend_events.py`、`dividend_event_run.py`、`quantity_cash.py`及
历史规则组件，对既有原始响应做离线事件验入：区分重复、预案/实施和修订、应收与到账、
送转与可卖日期，并补2020—2022规则覆盖。届时仍须明确哪些只是回溯观察，哪些能用于
历史账户重建或信号；不能靠重新下载相同资料来制造更早的观察时间。完成必要验入后，
才接固定S4信号的调度和扣费情景，不重跑本批或已经完成的S4排序诊断。
