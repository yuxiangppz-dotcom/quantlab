# S4 首条历史回放验入结果（只读核对，2026-09-13）

本报告只读取已封存材料，未重跑任何封存程序、未新增下载、未替换第 21 名、
未把任何空返回解释为零成交或停牌。结论先行：**在现有材料与本次授权范围内，
S4-A/C80 首条历史基线无法启动，停在执行前验入阶段**；闭环引擎本身已用
人工可手算情景验证（见 [risk_ledger_loop_zh](risk_ledger_loop_zh.md)）。
本报告不构成收益结论，未消耗经济路径预算。

核对来源（均在 Codex 目录，只读）：
`data/products/research_program/launch_20260912/s4_first_entry_plan/`、
`s4_entry_raw_precision/`、`stock_replay_inputs/`。

## 逐项验入结果

| # | 项目 | 状态 | 对回放的影响 | 现有材料能否解决 |
| --- | --- | --- | --- | --- |
| G1 | 000301.SZ 缺 2021-12-22、000777.SZ 缺 2021-12-07/08/09/10/13 的原始日线（prior20 成交额窗口缺口，共 6 个空日期） | 未通过 | 这 2 个提案的 `prior20_amount_fen` 未知 → 内核返回 `prior20_amount_fen_unknown` → 调度器 preflight 于 2022-01-04 整路径停止，当日全部 20 笔买入都无法执行 | **不能**：封存卡 13/13 次原始请求已用尽，空返回是供应商事实；解决需新的有界原始请求（本次授权禁止）或用户批准的明确降级决策 |
| G2 | 7 只股票 2022-01-04 执行日精确成交量（002006/002366/002505/002635/002805/300757/600961） | 数值已封存、未自动验入 | 不影响 preflight，但整数股容量换算需先正式验入这些精确值 | **能**：封存 reconciliation 的 raw_normalized 整数与 float 归一化复现一致，补一张有界验入卡即可，无需新下载 |
| G3 | 20 个提案的市场/历史身份资格、停复牌与涨跌停上下文验入 | 未通过 | `calendar_verified`/`market_open`/涨跌停字段未验入 → preflight 停止 | 部分：stk_limit 历史分区存在（1212 日），逐股验入未做 |
| G4 | 公司行为处理声明（2022-01-04 前后无未处理分红送转） | 未通过 | `corporate_actions_processed` 未知 → preflight 停止 | 部分：31 只分红缺口原始响应与 4516 股原始批次覆盖 20 提案中的 18/20（s4 执行前置审计），逐笔核对未完成 |
| G5 | 附加费用（过户费等）范围 | 未确认 | 费用场景不完整 → `fee_components_unknown` 阻断 | 不能擅自填零；佣金万 0.86/最低 5 元与卖方印花税可据实使用 |
| G6 | 142 个未知代码-日期的成交额精度（56 缺行 + 86 分尾差） | 已有解释 | 分尾差为二进制浮点换算痕迹；保守容量下界方法已在 #113 另立卡处理 | 已按封存解释保留 |

## 停止点与最小缺口清单

停止点：2022-01-04 执行前验入（第 1 个执行日，尚未产生任何模拟成交）。

最小缺口（按阻塞优先级）：
1. G1 的 6 个空日期：需要用户新授权一次有界原始请求重试，或明确冻结
   "缺口日按保守容量下界处理"的降级规则（二者都超出本次委托范围）。
2. G2 验入卡：纯本地、零下载，可立即立卡。
3. G3/G4 的逐股验入：本地已有分区，属有界人工/程序核对工作量。
4. G5：等待用户提供券商实际费用范围，或按已声明佣金+印花税的
   研究情景命名并封存（不得称为全成本）。

以上任一未闭合前，不启动 80 条经济路径中任何一条；本次闭环 PR 的
人工情景测试不是历史回放结果。
