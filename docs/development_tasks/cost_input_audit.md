# Issue #72: saved-score cost and corporate-action input readiness

Direct user commission, continued by the authorized two-hour heartbeat. Start at
clean pushed master `62f8489cb7cea5a1bbb84538732d72166a035962`, exact CI
34603114572 successful. No concurrent executor/reviewer or heavy research job
was found; the old shared reviewer remains paused. Branch
`codex/cost-corporate-action-input-audit`.

The task says six evaluation-score products. Inspection of the sealed model
receipts identifies six model fits but **eight** relevant Parquet files: six
2023/2024/2025 evaluation files plus the final two models' observed-2026 files.
Use all eight to preserve the declared 4,442,616 rows per model and 895 sessions.
This is a clarified physical file count, not an expansion of dates or models.

Bind the existing rolling report, original Git source, fit/artifact receipts,
metadata, fixed historical calendar and independent v2 rule report. Verify their
hashes before reading score values; never deserialize a model or calculate labels.
Read only identity, date, complete-feature flag and score. Reconcile the complete
metadata grid exactly against each model's saved score population, and record
incomplete and absent metadata separately. The original model experiment's Arrow
coordinator limitation remains historical; this task does not refit to remove it.

Reuse the fixed-count constructor with deterministic ascending-code tie breaks,
at most 20 equal targets and 80% gross exposure for each model/day. Persist the
intentions before inspecting market or corporate-action inputs. No replacement
based on future data is permitted. These independent daily intentions are not
orders, fills, a continuous cash portfolio or realizable returns.

Check next-session entry and entry-plus-5/10/20-session intended exits. Include
entry through exit in the required daily/adjustment window; preserve cutoff tails
as unknown even if a later current calendar happens to exist. Inspect a bounded
union of required instrument/dates, including the signal-date adjustment reference.
Record missing partitions, wrong dates, duplicate keys, invalid OHLC/adjustments,
and adjacent adjustment changes. Valid daily prices or adjustment factors cannot
establish a fill, tradability or a corporate-action cashflow.

Profile all local dividend observations and retain first observation of repeated
content identities, while marking payload conflicts unknown. Check optional-field
nulls, invalid values, record/ex/pay ordering, implementation versus proposal,
positive-cash/pay and positive-shares/listing requirements, and observation versus
availability. Inclusive overlap of record/ex/pay/share-listing dates creates only
a review candidate. A record date at an intended buy/sell boundary does not prove
ownership, and a payment after intended exit still requires accounting. No exact
entitlement, dividend tax, complete cost or execution authority is emitted.

Six official public documents are saved and hash-bound: Tushare dividend field
definitions, stamp-tax law, 2023 halving, 2008 sell-side change, and 2012/2015
dividend-tax notices. Five initial Python HTTPS attempts failed certificate-chain
verification with zero bytes; Windows' trusted HTTP stack retrieved the same URLs
successfully, without disabling verification. All failures remain in the journal.
Tushare is consulted only as public documentation; no market-data API is called.

The provider defines cash and share distributions per share; base_share uses ten
thousand shares. Raw request/payload, base fields and event-history completeness
are not invented where canonical records omit them. Dividend tax requires actual
account lots, FIFO/end-of-day net changes and calendar-month/year rules; 20 market
sessions is not automatically one month. The 2015 notice changes the older rate
and collection rules; this audit does not implement a new tax ledger. Use the
frozen user commission profile and its dated stock sell-side stamp component for
separate single-order hypothetical notional illustrations across the fixed dates
and CNY50k/200k/1m scenarios. Rounding and per-order aggregation remain assumptions,
the other fee coverage stays unknown, and ETF trading is outside this audit.

Pin the new contract and input manifest at a clean pushed commit before one full
audit. Produce aggregate findings, finite examples, compact target/window/market
Parquet companions and the exact missing-input request. Independently reconcile
populations and representative boundary cases without calling the audit helpers.
Then add a read-only Chinese workbench component, downloads and findings.

Limits: additional public sources 50 MiB, generated outputs 512 MiB, RSS 4 GiB,
explicit computation and Arrow CPU/I/O pools of two, one heavy job, host D reserve
8 GiB and heavy work at most 45 minutes. No real-data model fits, score/factor
recomputation, provider calls, canonical writes, performance backtests, account or
order changes, invented fills, forward registration, promotion, C cleanup or
legacy default-rule changes. Full pytest, all five optional synthetic runtime
tests, Ruff, diff check, browser acceptance, PR/green CI/merge/master CI and an
honest delivery with a single next task are required.
