# Closing-auction quantity subset for the fixed 2022–2024 comparison period

Direct approved v3 commission. Exact clean pushed expected_head:
d397660b828fd9ae8e14a4ed5c362437a852f203. PR114/master CI34733859170 passed both jobs.
No old coordination writer, heavy lock free; shared loop remains paused. Push card
before source review, new catalogue implementation or real lookup sweep.

Resolve one necessary historical replay gap: whether the already reviewed tick,
minimum/increment/maximum limit quantity, whole odd-lot exit and T+1 subset also
applies to common-stock closing-auction LIMIT orders in the fixed2022–2024 period.
Do not extend into market orders, closing-price after-hours trading, block trading,
price cages/bands, account eligibility, actual auction liquidity or verified fills.

Reuse exactly eight existing original documents and their saved extracted text:
sse_2020_rules, sse_2023_rules, szse_2021_rules, szse_2023_rules,
sse_star_2019_rules, szse_chinext_2020_rules, sse_2014_sell_notice,
szse_2014_sell_notice. Revalidate the inherited catalogue's already bound effective
date/amendment/deferred source chain without repeating its old actual/proof writers.
Check relevant original pages/paragraphs and quotations, including closing-price
fallbacks and the distinction between a permitted order and an executed auction.
At most2 targeted official searches and2 official page opens for this new question;
zero new file downloads and zero retries of any earlier failed URL. Save receipts.

If supported, create a separately named closing-only wrapper, preserving the exact
old default/continuous resolver payloads and all unsupported None results. New
closing rules must cite the precise provisions, retain the parent's effective
boundaries and inherited limitations, and keep execution/performance authority
false. Unsupported or unverified scope stays None. Bound raw/text hashes, all
used config/manifest/old report+proof and exact source calendar before one new
lookup sweep. Audit only the original common2022–2024 calendar and four boards;
one new actual and one independent source/numeric/delegation proof. No old runs.

The approved plan already says daily execution is an explicit simulation: ADV20
1% conservative and5% sensitivity are capacity assumptions, not closing-liquidity
proof. Preserve that distinction; absence of tick-level proof is not silently
turned into fills, nor into an invented requirement that every conditional daily
study have tick data. Actual session facts, corporate cashflow and full economics
remain separate. No S4 signal/rank/NAV/fee-scenario rerun, provider call, canonical
write, model fit, economic path, candidate expansion or strategy promotion.

At most35 minutes useful work. One heavy lock,600-second actual,2GiB RSS,16MiB
output,8GiB physical D reserve. Full pytest/Ruff/diff, source and self-review,
commit/push before actual/proof, PR/CI/merge/exact-master CI and concise findings.
If the documents do not support the subset, publish that bounded blocker and
continue the next independent approved route; do not keep expanding the search.
