Start from exact clean pushed master0f9b8a367bf6fbb865e0bbbfb1140fd6967c6e95 only after exact master CI34674163452 succeeds. Read AGENTS.md, mandate, extended_frequency_plan_handoff.json, docs/extended_frequency_plan_findings_zh.md and docs/extended_economic_evaluation_spec_zh.md. Direct single-agent commission; no concurrent writer/heavy job; old reviewer loop remains paused. Preserve source branches and all old artifacts. Keep two-hour heartbeat/about90-minute effective work with30-minute closure buffer; do not start a segment that cannot safely checkpoint before the end of the work window. The task may legitimately span several heartbeats, with one exact pushed processing source and one cumulative fit ledger; a checkpoint must record that source HEAD and next unused slot. Resume only that matching source, never a different HEAD or duplicated runner.

Issue #82 / PR #83 completed the zero-fit extended plan at source05bb8b700b8828a15a2d86a24afbd358c97d1739. Bind reportd57dfcc7a0ed747a07a94993e723267584dd8757deb95586660f7f387e2f0bd7, schedulecb6f3fc3b552609f85d2868fdeebe271aa9083cfe636a6ed625afc7ae32bdbae, independent0d23c82471adaafacb70c3e672dc713e7830bc13439dd16123e8f8c068384f43 in data/products/extended_frequency_plan/extended_plan_20260912. The plan has52 trading weeks,242 market days,12 monthly anchors,104 required unique weekly-family models,6 exactly reusable old pilot fits and98 maximum NEW cumulative fit attempts. Planning executed0 fits and generated0 predictions. Original pilot report65d8c1f67c14b5110a8745b5a16a5b243a64045f7683961e892b55e8fdc833ca/independentef34033175228aed9d3fd3da8bd789ac8e19ef31fed7a4f8b4f337ce9d80fb22 at source0a29495fa37d767ffb7d4431180ecff54abf74c5 stay immutable; its6/6 budget is closed and cannot be reset.

Bounded commission: implement, validate, commit/push, then execute the EXACT extended Alpha158 frequency replay across resumable capped segments. The user authorizes local training and ignored experimental predictions/diagnostics for this declared batch, id alpha158_extended_frequency_20260912. No provider calls, documentation/data downloads, new feature materialization, canonical/account writes, orders/fills, cash/tax/return postings, actual portfolio backtest, strategy promotion or forward registration. Do not change dates, feature population, labels, model parameters, seeds or preprocessing to improve outcomes. Keep Alpha101/191 and economic accounting implementation outside this task; the next implementation should use this saved-score evidence for the already-fixed minimal economic evaluation.

FIRST implement and validate the cumulative runner, before any actual fit. Reuse pure verified matrix/model adapters where suitable, never modify/reconfigure the old6-fit runner or snapshots. Seal the new config, input/runtime/source inventory, full52-week schedule, per-slot train membership hashes/counts, prediction identity unions, feature order, fixed model parameters and all budgets in a clean pushed source commit. Reconstruct training hashes for every NEW slot by streaming fixed metadata; do not build all matrices together or write fresh copies of the11.79GiB feature store. Match the planning counts and exact global five-market-session label endpoints. Keep complete158 feature membership; label availability only controls training/evaluation, never whether a prediction member exists. Every train label endpoint must be<=that slot's fixed cutoff. Fresh float64 training-only mean/std for Ridge, constant scale1, float32 matrix/labels, no label transform; unchanged LightGBM identity preprocessing,100 fixed rounds/no early stopping/search and original seed/runtime.

Schedule is immutable: signal2025-09-01..2026-08-31, label evaluation cutoff2026-09-07, exact52 ISO-week groups including holidays/final one-day week. Each slot uses last market session before its first signal day as cutoff, three-calendar-year rolling start. Weekly policy uses each weekly candidate; lower-frequency policy reuses the first weekly candidate whose first signal day belongs to each month until next monthly reset. This is explicitly 'first weekly refresh of each month', not training on unavailable calendar month-start days. Do not change the month/ISO-year mapping after inspecting outcomes.

Use a distinct global NEW-fit ledger with exactly the98 non-reuse slot identities in the saved plan, in chronological week then Ridge/LightGBM order. All failures, abandoned invoked slots and retries consume durable attempts; no automatic retries or99th slot. Original6 refs live in a separate immutable reuse manifest and never consume/replenish new-fit counters. Seal started/fit-intent records before model.fit, bind source/plan/slot, and never fit a consumed or completed slot again after restart. Incomplete slots without trusted receipts must stay failed/blocked rather than be guessed successful or silently rerun. Keep both claimed/consumed attempts and actual fit invocation counts. Finished models/outputs can only be verified/reused. Stop on a failed model and report the remaining unexecuted scope honestly; do not fill its results with a more favorable substitute.

Resource limits: one shared inherited OS heavy-job lock plus experiment lock surviving coordinator loss; exactly2 compute and Arrow CPU/I/O threads; a bounded owned worker/process tree per fit, <=8GiB combined RSS, >=10GiB available before each fit, <=8GiB cumulative new experiment/runtime files and host D reserve>=8GiB. Each segment<=45minutes, no new fit after38minutes, maximum6 new fit attempts per segment. A heartbeat may process another short segment only after the prior segment has closed and enough of the90-minute work window remains. Segment boundaries never reset the98 total. Put all logs/cache/MLflow/temp/model files under D-backed project data paths. Do not change bound source while workers run. Keep source frozen across unfinished segments; defer presentation-source changes until fitting is closed, or use an explicitly verified isolated frozen-source runtime without copying/overwriting canonical data. Never terminate an unrelated job or steal a lock.

Prediction contract: each weekly-family artifact covers the UNION of its own week's members and every additional week for which it is the monthly anchor. Monthly first weeks share the same artifact, no extra fit; bind exact ordered identities and all required dates. Summed logical per-model-union artifact populations must be4,320,562 rows across104 required model references. Policy views each have1,215,709 members and1,214,290 evaluable labels, per family/policy. There are54 shared signal days and188 actual frequency-comparison days per family. Preserve old6 model/prediction/preprocessing files. Reuse verified existing predictions where exact rows/features/labels match; any expanded predictions from an old model go only to new derived artifacts, and report reused versus newly computed row counts. No model refit is permitted for an expanded prediction horizon. Save preprocessing/model hashes, Qlib run identity, actual trained_atUTC, replay cutoff/start, iteration/round count, elapsed/RSS and all outcomes. Reload each new saved model and reproduce ALL its saved prediction values without an extra fit.

Complete policy diagnostics only from all required verified artifacts, or clearly label checkpoint/incomplete coverage. Stitch all242 signal days before rank-stability/top20 membership metrics so monthly/weekly model changes remain visible. Expected full output:968 policy-day rows,208 family/policy/week summaries,48 family/policy/calendar-month summaries and80 paired non-shared-week comparisons. Monthly aggregation is by actual signal calendar date, retaining cross-month weeks; frequency pairing excludes the54 shared days by the fixed week mapping, not by observed performance. Exact matched stock/date members and labels are mandatory. Keep missing/undefined correlations, coverage counts and negative results; no independent-sample p-values on overlapping five-day labels, no conversion from RankIC to accuracy/return. All history is already observed, actual training happens now; no backdating of knowledge, 'new holdout', causal market explanation or live refresh claim.

Validation BEFORE fitting: full uv run pytest, Ruff/diff and all7 optional runtime tests; meaningful synthetic tests for global98-slot accounting/reuse6, consumed-slot crash/resume, inherited locks/owned worker caps, unavailable resources, exact union identities, monthly/ISO-year/partial-week mapping, mature label/scaler membership, label-independent predictions, boundary continuity, null metrics, model/source/receipt tamper, saved-model replay and missing evidence. After processing, independently verify every training hash/count/scaler, all saved predictions/union identities and all daily/weekly/monthly/paired aggregates without production diagnostic aggregators. Save a Chinese explanation and read-only UI with actual cumulative consumed/completed/failed/reused counts, dates, complete outcomes and downloads; replace any current-status wording about 'not started' while preserving historical zero-fit planning reports. Browser acceptance, exact green PR CI, merge/masterCI and full AGENTS.md delivery with start/final HEADs, pushes, changed files, measured resources, failures/limits, attempts across heartbeats and one next card.

Do not rush extra fits to meet a wakeup or fill time. If source setup or verification consumes this work window, stop at a clean pushed checkpoint with0 new fits and resume correctly next time. If98 fits require multiple windows, keep the pending PR/source branch and same cumulative ledger; do not claim the whole task complete early. The strategy-first question is whether the longer fixed comparison shows useful/stable prediction before the minimal cost-aware portfolio stage; no automatic winner or complex risk overlay, and20percent drawdown remains unverified.
