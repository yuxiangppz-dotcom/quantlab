# S4 fixed first-entry raw-unit reconciliation

Direct approved v3 commission. Exact clean pushed expected_head:
72b12772d5283c560ccdb08c921a8fa277291320; PR121 and exact-master
CI34749801400 passed. No concurrent writer in this workspace or heavy worker.
User delegated the separate market-risk modules to ZCode in an isolated checkout;
do not modify those modules or the old shared-loop mailbox/SQLite.

Freeze and push this card before implementing or requesting new source rows.
This part addresses only numeric prerequisites in the saved S4 first-entry plan,
not a new signal, a rerun of that plan or the whole earlier prerequisite audit.
The original 256 population, 20 proposal ranks, decision 2021-12-31, execution
2022-01-04, nominal sizing and 1% capacity scenario remain frozen. Existing
canonical values and sealed actual/proof results must remain byte-for-byte intact.

Authorize one finite fresh raw Tushare daily intake, exactly these13 code/dates:
2022-01-04 volume precision: 002006.SZ,002366.SZ,002505.SZ,002635.SZ,002805.SZ,
300757.SZ,600961.SH. Prior20 missing amounts: 000301.SZ on2021-12-22;
000777.SZ on2021-12-07,08,09,10,13. The latter six missing dates are not zero.
Each request uses one ts_code and matching start_date=end_date, fields
ts_code,trade_date,open,high,low,close,vol,amount; the first planned request is
the permission check, no extra probe. Maximum13 requests,0 retries,100/minute,
6000 provider cap and at most1 row expected per request. Empty results are saved
unknown observations, not evidence of zero turnover or normal/suspended status.
Do not expand to other codes/dates, change rank membership or backfill canonical.

One official documentation open of https://tushare.pro/document/2?doc_id=27 is
allowed before intake to verify endpoint parameters, raw units and limits; no new
search query, other-page expansion, download, retry of failed pages or use of old
closed search allowances. Save a bounded documentation receipt. If the documented
contract conflicts, stop before provider calls rather than inventing a conversion.

Bind original first-entry plan/report/proof, original input observations and
liquidity projection/report/proof, relevant canonical source partitions, original
calendar/selection, official documentation receipt and code/config hashes.
Use existing WireClient/Journal/acquire transport with raw response preservation.
Keep exact byte hashes, code/date keys, attempt ordering, schema/truncation failures
and source capture time. New snapshots cannot certify original historical timing,
absence of revisions, tradability, identity or corporate processing.

New pure conversion must parse raw JSON decimal numbers exactly. Convert provider
vol lots to integer shares by100, and amount thousand CNY to integer fen by100000;
verify actual documented units first. No float-nearest rounding, epsilon guessing,
flooring volumes, double conversion of canonical fields, or price rounding.
Finite nonnegative values off the integer grid remain unknown with a reason.
For returned existing-bar dates compare normalized raw OHLC/amount/volume against
the saved canonical observation, retaining discrepancies rather than rewriting.
A different decimal representation is not automatically a changed historical fact;
report exact and float-representation comparisons separately. Do not infer that a
nearby integer was the original observation without the new raw-source evidence.

Produce a13-row reconciliation preserving every requested key, even when empty or
invalid, and a20-proposal prerequisite summary that copies ranks and original
unknowns. Mark new exact source numbers as separately observed alternatives, not
automatically admitted scheduler values. No scheduler run, NAV, return, drawdown,
new candidate/model/formula/funding identity, economic path, external order or
USER_APPROVED promotion. Necessary fee/status/corporate/identity gaps stay unknown.

One actual intake and one independent raw Decimal/Fraction reconciliation/proof,
no rerun or resetting attempts after failure. Each worker<=600seconds,2GiBRSS,
64MiB total generated output,8GiB physical D reserve,one heavy lock. Individual
response<=2MiB,cumulative raw bodies<=16MiB. Finish implementation/check within
35minutes of starting this part; respect this heartbeat's75/90minute closeout gates.

Tests: exact raw units, fractional off-grid and boolean/string/nonfinite values,
empty/duplicate/wrong keys/extra rows/truncation/provider permission errors,
no original value mutation or signal reranking. Independent proof reads all13
requests/raw bodies directly and checks every reconciliation output without
calling the writer/converter. Same-agent alternate arithmetic is not independent
personnel review. Full pytest/Ruff/diff,self-review,commit/push before actual/proof,
then PR,CI,merge,exact-masterCI or honest pending handoff at the time limit.
