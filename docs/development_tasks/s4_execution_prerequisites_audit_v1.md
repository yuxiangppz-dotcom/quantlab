# Bounded S4 net-cost replay prerequisites audit

Implementation base: clean pushed master be1e721ae925f61af62a731185d7cc6814bfedd4,
exact master CI34712402334 passed.

Authority: approved v3 plan and the completed S4 signal task. Read-only audit,
not a portfolio replay. After PR99 merges, bind its exact clean pushed master in
the started receipt and confirm no concurrent writer or heavy worker. Do not
change old Issue90's input-audit attempts, old output, budgets or mailbox.

This card authorizes local source/schema/coverage inspection and bounded official
public-document review, ignored audit files and a Chinese findings document.
It authorizes zero Tushare requests, model fits, formulas, strategy variants,
economic paths, canonical writes, orders or promotions. S4-A/B/C remain unchanged
regardless of their IC results. No return, trade or NAV is computed by this audit.

Use the same fixed256-stock cohort and 2019-10-08 through2024-12-31 input horizon.
Goal: identify exact requirements for S4-A baseline/stress net-cost research,
particularly exits that cannot fill. It is not a requirement to finish every live
account/UI module. The user-reported0.86per10000 commission,minCNY5 for stocks,
excluding stamp tax, is a declared research cost input, not a verified broker bill.
Do not double count charges already included in the declared all-in commission;
record any unresolved composition. CNY200000,no leverage,at least one session held.

## Finite scope

First segment maximum30minutes,2GiBRSS,256MiB output,8GiB physicalD reserve.
- One local directory/schema inventory may list all canonical and staged research
  dataset paths. Do not read price/return data beyond the bounded sample below.
- Read at most30 tracked implementation/config/limitation/task files relevant to
  execution simulation, order sizing, fees, market context, corporate actions,
  dividend raw receipts and baseline evidence. Record paths and hashes.
- Read at most20 existing local Parquet/JSON data files selected from the fixed
  dates2020-01-02,2022-01-04,2024-02-05,2024-12-31,2026-09-11 and from already
  downloaded dividend/context evidence. Prioritize market-context/limit/status
  schemas; listing and file-level metadata are allowed without loading all prices.
  No provider sampling or large canonical scan. Missing date/file stays missing.
- At most10 targeted official public searches and20 distinct official public-page
  or PDF GETs. No automatic retry after timeout/denial; retain failed attempts and
  actual source URLs, publication/effective dates and observation/hash receipts.
  No paid independent endpoint, anns_d, API alias or data-provider call.

## Questions the report must decide

1. Inventory actual historical price-limit, suspension/ST/lifecycle and corporate
   action coverage for this cohort/horizon. Does the simulator preserve unknowns
   and locked holdings, or currently permit unconditional next-day close fills?
   An absent suspension/ST row does not certify safety. A raw daily bar's existence
   is not a queue or auction fill. Do not fill missing rules with2026 engineering
   assumptions, assume permission across all boards, or erase delisted holdings.
2. Identify where prior20-session amount participation caps,100-share versusSTAR
   minimum order/increment rules,odd-lot exits,T+1 and same-day cash reuse would
   enter a specifically hypothetical quantity/cash replay. Verify historical rule
   effective dates from exchange sources; unknowns remain unverified.
3. Verify effective-dated stock seller stamp tax in2020–2024 from official fiscal/
   tax sources. Separate the user's stated all-in commission from extra tax.
   Slippage and participation remain disclosed modeling assumptions; do not claim
   observed spread,close capacity or guaranteed fills. No free cash yield added.
4. Does available dividend/split evidence support raw quote quantity/cash accounting
   without double counting adjusted returns or spending unpaid receivables? Only
   facts relevant to replay are needed; no demand for full textual announcements.
5. Produce a concrete input-to-module readiness matrix: available and sufficient,
   available but unverified,missing,or implementation work needed. Existing code
   availability and historical data adequacy are separate columns. State whether
   an honest historical net-cost S4-A replay can currently run. If no, freeze exact
   blockers after this finite audit and specify the smallest independent next
   task that can remove one blocker. No infinite permission loop or repeated audit.

After the audit seal one report, source ledger and separate read-only verification.
Do not reread or recompute completed S4 signal outputs for a new result. A new
bounded source-intake or replay-implementation card can follow within the approved
plan, but must state exact endpoint/request/bytes/candidate/path limits and be
committed before it runs. Provider absence or unusable history must not become a
favorable inference. No economic path is consumed or authorized by this audit.

Read-only checks may narrow to source hashes,independent coverage/rule mapping and
git diff --check. Code changes would require focused/full pytest,Ruff/diff and
commit/push/PR/exact-master CI. Report actual attempts,failures,fixed assumptions,
remaining limitations,starting/finalHEAD and specific freeze/next-step recommendation.
