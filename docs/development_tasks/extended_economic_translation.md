# 固定四策略的最小经济转译（Issue86）

起点：干净且已推送的 `b26712ec40362691062548d0c271484b2fc912c2`；
主线 CI34690106314 通过。按直接单代理委托执行，旧共享审核循环暂停。
先检查已有进程和两把持久锁，再编辑；保留全部原失败、恢复、模型和预测字节。

使用完整年度报告 `729926c1c8276af6834fbe7536ac4fbb87b056cad79039930b6c99dbacdb4cd8`
及独立证明 `2447285f9c3f16223493f69167ce977dc09940076e8d8acd002731e9379f50de`。
原98次新训练预算已全部消耗；本卡新增拟合预算为0。

按事前冻结规格保留四个策略和独立命名的80%预测成员等权对照，5/10/20市场交易日
调仓间隔、额外单边摩擦0/5/10/20基点，共60条唯一标准化价值路径；假设资金仅线性缩放。
固定2025-09-01相位，信号至2026-08-31，观察至2026-09-07；下一市场交易日复权收盘价
价值转移，不强制末日平仓。保存全体成员和负面/未知/停止结果。

输入来自已封存预测文件和历史原始月度分区。预先生成输入哈希、完整15套目标序列哈希、
标记价格哈希、日历、生命周期与风险事实绑定，再测试、提交、推送；开始实际情景前必须
使用干净且已推送的准确源码。采用冻结的legacy生命周期、严格停止、不启用结算假设，
并对全部策略/对照沿用现有 `exit_after_termination_decision_v1` 与固定
`config/delisting_facts.json`。这个事实文件仅覆盖其已有核验记录，不意味着其他公告不存在。

每条情景只能预约和调用一次；完成结果可跨轮复用，失败与中断不自动重试。只允许新派生
研究目录写入，不调用数据源、不写正式数据/账户、不提交订单或虚构真实成交。
一个重任务、两线程、合计内存不超过8GiB，每条开始前至少8GiB可用，产物累计不超过2GiB，
D盘至少保留8GiB；作业段45分钟、38分钟后不再启动情景。每轮约90分钟并预留30分钟。

验证目标不依赖未来标签和可得价格，现金与持仓守恒、费用自融资、无价冻结、不可买留现金、
首相位/节假日/延迟、末尾持仓、生命周期停止、控制组分母、资本尺度不变。
全量pytest、Ruff、diff检查；完成后独立重建全部目标及每天价格/持仓/转移/费用恒等式，
核对全60条状态与策略/对照对称性，中文页面和下载实际验收，绿色PR CI后合并并检查主线CI。

只报告明确标注的复权价格研究代理和“扣除声明额外摩擦后的价值”，不称为完整用户净收益。
不包括已验证的逐单最低佣金、卖方印花税、整股/T+1成交能力、权益批次/股息税现金账户；
不将额外摩擦0解释为真实免费交易。未知历史全市场覆盖不改成已证实，也不把观察历史称为
新前瞻。不作市场下跌因果或20%真实最大回撤达标结论，不自动晋级策略。

下一阶段复用这些保存目标，做最小版本化数量/现金与实际收费情景适配；不新增模型或风险规则
来美化已经观察的历史。
