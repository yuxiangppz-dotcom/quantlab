# S2-A prospective valuation observation, one fixed candidate

Direct approved v3 commission; exact clean pushed expected_head
d54f73c5ca5c282b6522a50fdb37270956912ddf, PR115/master CI34734759877 both passed.
No coordination writer; old shared loop paused, single research lock. Push this
card before formula implementation, computing this new score or registration.

The bounded S2 historical audit is already blocked on denominator revisions. Do
not repeat it or claim that later retrieval repairs historical PIT. Advance the
approved prospective alternative using the already saved Sep11 daily_basic raw
snapshot, received2026-09-12T17:36:08.992141Z, with the original fixed256 cohort.
No downloads, provider calls, canonical writes, old score reruns or future prices.

Freeze exactly one S2-A identity. This consumes one further rule candidate, moving
3/15 to4/15, with0 economic paths and0 fits. It does not create B/C variants or
enter historical rankings. New weekend observation is separate from the existing
S4 trial; do not change S4 source bytes, report, proof, dates or score.

Formula before results: use raw positive finite pe_ttm and pb, equal weight; negative,
zero or missing values remain unknown. Form five size strata across the fixed
cohort with finite positive observed circ_mv (raw10000CNY, no rescaling needed for
ranking): stratum=min(4,floor((average ascending circ_mv rank-1)*5/N)). Ties share
the same stratum; no identifier tie-break or industry claim. Within each stratum,
require at least20 stocks with valid pe_ttm and pb. Each eligible score is
(average ascending rank(-pe_ttm)+average ascending rank(-pb))/(2*eligible_count).
Require at least20 valid size observations overall. Keep every original code and
its reasons, raw source values, group and masks; never substitute zero or remove
loss-making/delisted/missing members from the output. This is a value signal,
not a profitability/quality or tradability certification; scale grouping is based
on values actually observed now, not historic membership reconstruction.

Register actual aware current creation time on2026-09-13 after source receipt and
before2026-09-14 09:30 China. Price/valuation asof2026-09-11; earliest hypothetical
entry closeSep14, evaluation label adjusted close of its20th subsequent common
SSE/SZSE trading session divided by entry close minus1. All21 contiguous session
prices and source availability must be present for a later eligible label; fixed
cohort, same finite pairs, minimum30, no cash/fee/net-return claim. No label or
correlation is computed now. The currently saved calendar stopsOct15, so explicitly
record that the exact end date is not yet certified; the20-session ordinal rule
is immutable. A future finite calendar/input/evaluation card resolves that date,
without shortening the horizon or substituting calendar days. No weekly re-score
or fits in this card. Historical S2 remains PIT_UNVERIFIED.

Before actual, bind exact raw/result/intent, old intake report+proof, selection,
and existing calendar bytes in a committed config. Independently validate raw
response hash, code/date/field uniqueness, source status and timing. Reuse only
the Sep11 snapshot; neither inspect other historical valuation rows nor re-run the
43-call intake or its proof. Build a pure scorer with synthetic tests and one
separately sealed actual registration and arithmetic/identity proof, not a
live/account Forward Shadow entry. One attempt each; no overwrite.

Budget:35 minutes useful work,600s actual,2GiB RSS,16MiB generated output,8GiB
physical D reserve, one heavy job. Full pytest/Ruff/diff and self-review,
commit/push before actual/proof, PR CI/merge/exact-master CI. No orders, NAV,
economic scenario, provider, model fit, user-approved promotion or budget reset.
