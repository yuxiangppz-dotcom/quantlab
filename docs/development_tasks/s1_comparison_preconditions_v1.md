# S1-A / S1-B comparison: frozen design, unresolved admission

Authority: approved v3 plan and the ETF event-audit handoff. Status:
**PRECONDITIONS_UNRESOLVED — zero economic runs authorized by this document**.
The result must not be launched simply because this file has been committed.
An exact admitted input manifest, rule-source ledger, reviewed implementation,
clean pushed HEAD and finite actual-run card must be sealed first.

## Fixed universe and chronology

The provisional pool remains 159915.SZ, 510300.SH, 510500.SH, 511010.SH,
518880.SH, representing ChiNext, CSI300, CSI500, five-year government bonds and
gold. Do not add or replace products after viewing return results. Current admitted
universe is empty: consult the hashed ETF event matrix, not today's surviving list.
Any exclusions must be written from identity/input/rule evidence before returns.
The comparison needs at least one A-share broad-equity product and at least one
non-equity class, with at least three complete years including 2022–2024 and a
fully known 60-session warmup. If that is impossible, retain the historical block
and use a separately registered prospective or independent-family route.

Use v3's eligible continuous evaluation window, at most 2020–2024; never move a
start date to hide a loss. Public historical reports may substantiate past event
accounting but cannot become earlier predictive information. Signals on the last
exchange session of each ISO week use only information available by that close.
First possible hypothetical execution is the next exchange session; holidays do
not mean a calendar-day holding rule. No same-day future labels/quotes are used.

## Fixed versions and controls

Initial capital CNY200,000, long only, no leverage, target gross at most80%,
minimum holding one exchange session for every instrument in this experiment.
No maximum holding period while a position remains selected; exits can remain
pending when execution is unknown or constrained.

S1-A ranks each eligible product's trailing60-session total-return strength,
using a causal event-accounted signal series. At most3 distinct underlying
exposures, equal target shares of the80% gross budget. Ties: ascending code.
If fewer than3 products are admitted, equal weight over the admitted selected
count. Do not count two products on the same underlying as diversification.

S1-B takes the identical top list and weights, retaining a selected allocation
only when that product's same trailing60-session total return is strictly positive.
Zero/negative goes to cash; missing information is unknown and not a cash signal.
Do not redistribute filtered weight or backfill lower-ranked products. This is
the one difference from A. F8 and an A-share index overlay are absent in both.

Controls, with no tuning: (1)80% equal by the same admitted underlying classes,
20%cash; (2)80%CSI300 ETF,20%cash. If CSI300 is not admitted, the second executable
control is blocked, not silently replaced by another winning index. Both controls
use the same weekly target/execution/cost conventions. The v3 daily mathematical
reference remains separately labelled non-executable and is not a simulated fill.

Reserve **8 paths**: A, B, diversified control, CSI300 control × baseline/stress.
They all count in the initial30-path strategy envelope, leaving22 paths (at most
11 two-scenario candidate runs); 28risk +16enhancement +6combination remain
unchanged, total80. This is a reservation, current actual use remains0.
S1-C and S2-B are currently unfunded historical candidate slots while their
specific evidence is blocked; do not silently promise both these extra pairs
within the remaining22. Their identities stay registered; any later reallocation
must occur before their run and preserve the80 cap. At most15 rule identities,
not an obligation to run all15 plus free controls. Failures count from actual start.

## Economics and gates still to bind

- Use unadjusted tradable quotations for research quantity/cash accounting.
  Never book raw-price dividends and adjusted-price returns twice. Cash dividend
  entitlement follows verified record-date holdings; separate ex-date receivable
  recognition from actual verified payment-date cash availability. Unknown dates,
  amounts, taxes, rounding or action identity block the affected replay.
- Split quantities require the actual declared event and aggregate-holder rounding,
  separate event and first quoted unit dates. Do not derive subscription flow from
  a split, and do not infer the user's quantity from total fund shares.
- User ETFcommission:0.86per10,000, minimumCNY0.10 per aggregate one-side order,
  excluding any applicable tax. Exact statutory ETF tax/fee applicability and
  historical changes, rounding, order lots/odd-lot disposal, maximum size and
  historical price-limit rules require primary evidence before admission.
  Do not copy the stock CNY5 minimum or silently set unknown charges to zero.
- Baseline/stress slippage assumptions:5/15basis points one side. These are
  research assumptions, not observed spreads or broker fill promises. Both use
  at most1% of prior20-session known turnover as an ex-ante amount cap, with all
 20 prior sessions required. A full day's volume does not establish closing
  capacity. Bound candidate quotes by raw daily range and verified limits;
  unknown status/limit/quote blocks an affected order rather than inventing a fill.
- Cash availability, order rounding and partial/unfilled target differences must
  be explicit. Do not spend unreceived dividends or assume unsettled sale proceeds
  are immediately available. Valuation cannot silently erase an unexitable holding.
- Cash earns0 in this comparison, a registered reference assumption. Do not add
  an assumed reverse-repo/money-market yield. NAV marks and order prices remain
  research hypotheses, distinct from orders, broker reports and live authority.

Before any actual path, make these execution assumptions machine-checkable with
causal synthetic examples: no lookahead, T+1/minimum holding, minimum commission,
cash/receivable conservation, once-only cash events, aggregate split rounding,
blocked exits and holdings surviving missing quotes. Bind actual inputs and the
exact available resource/attempt limits in the subsequent run card.

## Fixed decisions after an admissible run

Apply approved v3 section10 without curve-based exceptions: S1 net excess at
least1percentage point; both cost cases positive netCAGR, stress excess≥0;
drawdown no worse than the reference and≤25%; annual one-way turnover≤12;
positive annual excess in at leastceil(0.6*N), including2/3 in2022–2024;
full and fixed2022–2024 gates plus monthly concentration rule all apply.
The25% candidate research threshold is not the user's acceptable live loss.

Report whole/annual windows, net excess, drawdown, recovery, costs, target versus
executed turnover, missing/blocked orders and cash. Retain all run outcomes.
No USER_APPROVED promotion, untouched-holdout label, actual fill or future-return
claim follows from these historical comparisons. No return result exists yet.
