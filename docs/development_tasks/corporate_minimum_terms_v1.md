# Minimum corporate-action terms for the research controller

Direct v3 commission. Expected clean pushed base:
9bbe12171dfc121a788d0df417bf56eb4a862e7e, exact master CI34718394621 passed.
No concurrent writer/heavy worker; old shared mailbox remains paused. Commit/push
this finite card before implementation, then test/commit/push before one actual
staging run. Preserve PR103's strict readiness criteria and all sealed outputs.

Build a small typed research-terms adapter from the already verified occurrence
table, not a second raw parser. It must retain each of the 945 occurrences with
an observed event date in 2020-2024, including 924 implementation occurrences,
the exact duplicate and non-implementation stages. The population is frozen;
there is no return-dependent reselection, event deduplication or provider fetch.

Question: which minimum numerical/date fields are known for each component of
a future cash/quantity controller? A known zero total stock-distribution ratio
means no share-quantity addition for that observation. Its null stock-bonus and
capital-conversion subcomponents remain null, labeled not required for zero-total
quantity arithmetic. Any observed positive subcomponent conflicting with zero
total blocks; positive totals keep unknown breakdown explicit, and known partial
components exceeding the total block. Two known components must sum to total
within the original 1e-8 representation tolerance, which grants no fill authority.

Cash before tax must be an observed finite nonnegative value. Zero cash is
distinct from null; positive cash requires ordered record/ex/pay dates. Positive
total shares require a listing date not before ex-date. Record date must precede
ex-date for a complete term bundle; missing or malformed dates stay unknown.
All stages/conflicts/malformed source fields remain visible. No deduction from
the provider's cash_div field to the user's final tax rate or final net cash.

Output each component's known/unknown/inconsistent reason separately. A complete
numerical/date bundle is not an identified unique economic event, original
announcement, accounting entitlement or historical signal feature. All authority
flags remain false, and a required_context list explicitly retains event identity,
coverage, entitled holdings, calendar, tax and availability evidence. No cash,
shares, liabilities, dividends or NAV are posted by this adapter.

Finite I/O: bind the existing selection, PR103 report/proof/occurrences and source
definition receipt, plus this card (at most10 controls). Verify hashes rather than
rerunning either PR103 writer/checker. Up to1 official page open of
https://tushare.pro/document/2?doc_id=103 to confirm documented field meanings;
zero search expansion, zero provider requests or retries. Preserve failure if
the page is unavailable; use the existing hashed definition evidence only for
what it actually supports. Do not infer undocumented null semantics.

Run once with the OS heavy lock,600seconds,2GiBRSS,64MiB new ignored output and
8GiB physicalD reserve. One 945-row typed-term table, summaries by component and
an independent row/source-field checker. No old report/proof overwrite, canonical
write, formula/candidate/model/economic path, external order/account write,
strategy promotion, paid permission, C-drive cleanup or mailbox edit.

Test zero versus null, nonrequired subcomponents, partial-positive breakdown,
contradictions, date ordering, stages/duplicates, exact source linkage and all
authority flags. Full uv run pytest, uv run ruff check ., git diff --check,
self-review/commit/push, actual stage/independent verification, Chinese findings,
PR/CI/merge/exact-master closure. Target35minutes then proceed to another valuable
finite task, not another run of this one. Historical-rule coverage remains a
separate prerequisite; this does not certify historical backtest economics.
