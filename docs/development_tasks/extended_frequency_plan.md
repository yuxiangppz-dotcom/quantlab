Continue from exact clean pushed master f2eed392cb89dd5219b001bcda1492bfa6293be6 after exact master CI 34673098330 succeeds. Read AGENTS.md, mandate, docs/alpha158_weekly_pilot_findings_zh.md and alpha158_weekly_pilot_handoff.json. Direct single-agent commission; no concurrent writer/heavy job; old reviewer loop paused. Preserve every source branch, six-fit ledger and frozen artifact. Two-hour cadence with about90 minutes effective work and30 minutes closure buffer.

Issue #80 / PR #81 completed all six fixed weekly pilot fits at processing source0a29495fa37d767ffb7d4431180ecff54abf74c5. Report65d8c1f67c14b5110a8745b5a16a5b243a64045f7683961e892b55e8fdc833ca and independentef34033175228aed9d3fd3da8bd789ac8e19ef31fed7a4f8b4f337ce9d80fb22 bind the closed batch. All253806 predictions reproduced exactly, but no consistent weekly advantage was established from ten comparison days per family. Keep this outcome visible; no promotion, seventh fit or replacement of consumed slots.

Bounded next task: implement and run a READ-ONLY planner for a longer fixed frequency replay plus a minimal portfolio/cost evaluation specification. This task authorizes local calendar/metadata/source reads and ignored planning outputs ONLY, ZERO supervised fits, provider calls, documentation/data downloads, new feature materialization, model prediction generation, canonical/account/return/cash/tax writes, orders, fills, promotion or forward registration. Do not execute the planned future batch in this task. Keep all existing models/labels/metadata unchanged.

Predeclare the date rule before reading new comparison outcomes: the most recent12 complete calendar months of the frozen source ending2026-09-10, therefore signal sessions2025-09-01..2026-08-31. This is already-observed historical replay; the longer interval is for descriptive robustness, not a new holdout or causal claim. Five-market-session evaluation labels may extend through2026-09-07, available within the fixed source. Do not select dates from winning/losing months or omit partial/holiday weeks. Group ALL signal market sessions by ISO week, including the final partial week. Each candidate trains on the last market session before its week's first signal day; train_start=cutoff minus3 calendar years plus1 calendar day. Include complete158 features, finite labels with exact global5-session endpoints<=cutoff. Prediction members require complete features only; retain missing labels separately.

Fixed families/parameters/preprocessing remain exactly those in config/alpha158_weekly_pilot_v1.json. Weekly policy uses every planned weekly candidate. Lower-frequency policy uses the first weekly candidate whose first signal day falls in each calendar month, reused until the next such monthly reset. Explicitly name it 'first weekly refresh of each month'; it is not training on an unavailable month-start day. Both policies share that month's first candidate, and later weeks provide frequency differences. Stitch the full timeline for stability. The final partial week remains visible.

Enumerate every unique weekly-family fit slot and monthly anchor mapping; derive the total fixed future budget from this complete schedule. Reuse the six original pilot models only where feature/runtime/parameter/train cutoff/start, exact train membership/count and preprocessing contract match. They are immutable consumed fits, not six new budget slots. Verify existing model/prediction hashes, do not load/refit/re-predict in this planner. Any mismatch is a blocker, not grounds to silently rerun the old slot. New future attempts=all required unique fits minus exactly validated reuse; report both counts and per-segment cap6. The actual future runner must have one durable cumulative ledger for this complete declared budget, failed slots consume budget and no automatic retries. Do not clear budget by splitting weeks into differently named experiments. Per fit segment<=45min and no new worker after38min, one shared heavy job,2 compute/Arrow threads,8GiB combined RSS/10GiB available beforefit,8GiB output budget and8GiB D reserve; recommend restart-safe closure between segments.

Seal code, config, source inventories, all weekly dates/population counts, matched reuse source identities, monthly mappings, required/reused/new fit counts, forecast date/label coverage and explicit zero action counters. Aggregate fixed metadata for planning rather than allocating training matrices. Respect capped streaming reads, single shared lock, measure time/RSS/generated bytes and verify bound inputs before/after. Add independent calendar/membership-count reconciliation without using production planning aggregators. No performance metric recomputation or selection in this planning task.

Write a concrete minimal economic-evaluation specification, without computing economic returns here: same predeclared models/policies and signal universe; fixed top20 ranking with deterministic instrument tie-break; holding5/10/20 trading days and hypothetical capital50000/200000/1000000; signal close then next-session execution intent, T+1 and separate signal/intent/executable states; reuse the frozen lifecycle/portfolio/accounting rules. State how overlapping cohorts, cash, lot rounding, unfilled orders, fees, stamp tax, slippage sensitivity and dividend entitlement unknowns will be handled. Read existing fee/market-rule evidence and engine interfaces; do not invent verified rules or treat unavailable fees/entitlements/execution as zero. Label incomplete results as scenario/cost subtotal, not full net performance, and prevent later forward registration or live promotion without separate evidence. Identify the smallest implementation step that can evaluate economic value without changing the engine semantics; avoid more raw dividend backfill or complex risk overlays.

Tests: month/ISO-year boundaries, holiday and partial weeks, no omitted sessions, mature label purge and prediction membership independent of labels, exact six-model reuse, attempt accounting, source tamper, zero-fit/no model call boundaries. Required full pytest, Ruff and diff. Deliver Chinese plan/findings and a read-only UI with complete weekly schedule, monthly map, reused/new budget counts and downloads. Real browser acceptance. Each reviewable part must be tested/committed/pushed, final PR/exact greenCI/merge/masterCI, full heads/files/failures/resources/limits delivery and ONE next immutable card. Do not start actual extended fits merely because the plan is complete; next task must implement/validate the declared cumulative runner first. Strategy quality and cost-aware usefulness remain the main line, with Alpha101/191 and complex risk layers later.
