# One timely S4 price-only prospective observation

Direct v3 authority. Base clean pushed be0a2bd169b967c452a31a5fe09ac161f8efae09;
PR110 and exact-master CI34729762975 passed. Shared loop paused, no other writer.
Push this card before new data calls, new signal computation or implementation.

The chronological scheduler and fixed historical input bridge are complete as
components. Historical corporate/status/close-rule qualification remains blocked.
Rather than repeat their audit, record ONE new price-only S4-A hypothesis observation
before its future comparison window. This is no portfolio, net-return result or
strategy promotion. Old S4-A identity, original sign/three-session formula, fixed
2019 cohort of256 and the original negative20-session-change information reference
remain unchanged; no new candidate/formula/model or economic-path identity.

The observation is created on the actual current weekend,2026-09-13, using prices
through2026-09-11. It must never be backdated as a Friday Forward Shadow. Preserve
the old same-signal-day Forward Shadow admission policy and all its records. Use a
separate explicitly named weekend research-observation artifact with actual aware
created_at and price_as_of. Its entire future label interval must still be ahead:
next-session reference close2026-09-14 through the fifth later close2026-09-21.
Publication must finish strictly before2026-09-14 09:30 China; equal/late publication
fails and cannot be repaired by changing the timestamp, signal date or trial ID.
This local timestamp is not a trusted third-party timestamp service.

Acquire at most2 new provider responses, exactly daily and adj_factor for20260911,
once each, to isolated research storage. Current canonical latest date is20260910;
do not overwrite or update canonical data. Tushare credentials already exist in the
Windows user/process environment; pass securely to the WSL process, never print them
or put them in command arguments/files. Use the existing WireClient/Journal with
zero automatic retry or redirects,30 requests/minute maximum,8MiB per response,
16MiB body budget,row saturation6000. Empty/error/saturated/incomplete responses
remain terminal for this card; no endpoint aliases, retry, anns_d or paid permission.
Provider requests count in cumulative v3 (353 before this card).

At most2 official documentation opens and2 targeted official searches may confirm
daily/adj_factor fields and units. Save source receipts, including any failures.
Reuse the last20 canonical daily/adj_factor dates ending2026-09-10, plus the two new
responses, for21 exact calendar dates2026-08-14 through2026-09-11. Bind all40 existing
Parquet hashes/bytes, the source calendar and original selection before actual work.
SSE/SZSE calendar must agree, including the future comparison dates. Preserve all256
codes and invalid/missing observations; do not screen by 2026 survival or known future
returns. Raw quotes/adjustment factors may only create the same adjusted research
price features, never executable prices or dividend cash. Prior20 amount precision
issues from the separate cost-input bridge are not a prerequisite for this score.

Compute at price_as_of: adjusted P=raw_close*adj_factor on positive finite valid
OHLCV/amount bars; require all4 consecutive sessions for change3 and all21 for
change20. S4-A=median valid cohort change3 minus each change3; reference=-change20.
Save all256 scores/masks and exact inputs at once. No label, realized return, rank
selection, target weight, order, model fit, cash or NAV is computed now.
Record the planned later diagnostic only: after all6 future sessions mature, use
the same adjusted-price comparison P(t+6)/P(t+1)-1 with every intermediate price
valid, compare S4-A/reference Spearman on the same finite rows,minimum30. One date
cannot establish significance, stability or net profitability. That future input
acquisition/evaluation needs its own finite card; no current future-price reads.

Maximum35 minutes useful implementation segment,900 seconds actual worker,2GiBRSS,
64MiB new output,8GiB physical D reserve; one heavy-job lock. One actual start and one
separate scalar/median verification; no old writer reruns. Tests cover future-data
rejection, gap-preserving windows, unit normalization, stale/late timestamps,
immutable attempts, mixed calendars, schema/saturation/errors and no authority.
Full pytest/Ruff/diff, self-review, commit/push before actual, PR/merge/exact-master CI.
If provider data or timing prevents publication, preserve the failed attempt and
state that no prospective observation was admitted; do not invent success.
