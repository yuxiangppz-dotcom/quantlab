Start from exact clean pushed ea30299e8ff34c14c0010071ad1fdfbda790636e on codex/extended-frequency-runner, only after exact PR CI34685295344 passes. Read AGENTS.md, mandate, extended_frequency_failure_handoff.json, docs/extended_frequency_reuse_repair_zh.md and the original Issue84 card. Direct single-agent commission; old shared/reviewer loop remains paused. Verify no concurrent writer/heavy job and acquire shared and experiment locks. Keep the existing two-hour/about90-minute schedule and closure reserve. PR85 remains DRAFT; do not merge an incomplete annual experiment.

Original annual execution at fe11cff is TERMINAL FAILED and immutable: report1522e7b2bc40b33e2031901759ab09033b4df585657f46a12375be3b0de14071, failed reuse slot2026w32_ridge.94 successful new fits and3,944,760 predictions were independently reverified (proof127b68cae322a0f857ab87a9d6f0cd22d3da5358b4195dddce6b482301d1e6f8). No new fit failed; exactly4 new slots remain unused:2026w35_ridge/lightgbm and2026w36_ridge/lightgbm. Preserve all original attempts/receipts/models, the first failed reuse and global98-fit ceiling. Never rerun the original runner/worker, remove its failed report, change its status, create a replacement original attempt, reset/copy its fit ledger or silently skip the failure.

Bounded next commission: implement and execute a NEW VERSIONED, ZERO-FIT DERIVED RECOVERY for the SIX ORIGINAL OLD MODEL REFERENCES ONLY, under data/products/alpha158_extended_recovery/alpha158_extended_recovery_20260912. This is an explicitly commissioned repair validation, not an automatic retry of the consumed failed attempt. Source lineage must reference the failed original report and its declared slots, the94-prefix proof, original six-model pilot artifacts/source, and the numerical repair commit. No performance conclusions, full annual aggregates, model ranking or actual portfolio/economic replay in this part. No provider calls/downloads, canonical/account writes, new feature materialization, model training, orders/fills, promotion or forward registration.

Before any actual derived-score write, freeze/test/commit/push a recovery protocol, source/input/runtime inventory, exact six-job schedule and resource budget. Old forecast/model/scaler files stay byte-for-byte unchanged and only new derived artifacts are allowed. Carry an auditable global budget statement verified from original durable intents:98 maximum new fits,94 consumed/invoked/completed,4 unconsumed, THIS recovery0 additional fits. These numbers are not independent/replenished budgets. Bind the same original source-plan identitycfe72f5dec112918cc1ca325b4d950d883e6999a19486566065264438b253c45 and all fixed weekly model-spec identities, dates, preprocessing, features, labels and prediction unions; do not alter parameters/population after inspecting outcomes.

The six references are2026w32_ridge/lightgbm (106617rows each),2026w33_ridge/lightgbm (25375each),2026w34_ridge/lightgbm (25410each). Expected derived union314804rows:253806existing saved predictions preserved and60998new rows scored by the two original monthly-anchor models. No model refits. Together with the verified94 models this is100/104 references and4,259,564rows, still short of the full4,320,562 by the60998rows from the four untrained final slots. Missing coverage remains explicit and no complete-year comparison may be presented.

Preserve per-metadata-partition prediction batch geometry: exactly the original existing-row group in original order, and separately the newly needed rows in original order. Reuse repair logic only through a wrapper whose paths/identity are explicitly derived; do not call the original hardcoded worker or fit adapter. For each group record partition/row counts, ordered identity hashes and computation origin. Preserve all old score values and labels exactly; compute only missing rows; reload saved model bytes and compare every score bitwise using the SAME declared group geometry. Do not use allclose or silently re-score old values under a new kernel/batch size. Preserve constant/undefined metrics as unknown, although performance diagnostics are outside this part.

The observed first-partition numerical diagnosis9b4ab5a330565f3c2edbe6fe756f36a6be75fb22fc3f993fe10a14f262491b9d proves original390 rows re-predict exactly, but union540 changes1old/2new float32 results by up to5.59e-9. This does not prove all batches or all six models; the new recovery must verify ALL intended rows and retain any new failure. Its independent verifier must implement the declared two-group reconstruction separately, not reuse production predict_union or assume a different batch shape is bitwise invariant. Check original-model/scaler/file SHA, all old score/feature/label identities, new union membership, input/runtime/source hashes before and after, no fit invocation and unchanged original failed ledger.

Use one shared heavy OS lock and one recovery lock inherited by any child; two compute/Arrow threads;8GiB combinedRSS cap,10GiB available before heavy work,1GiB new output/runtime ceiling andD8GiBreserve. At most six declared prediction-only jobs, at most45minutes per segment, no new work after38minutes, no automatic rerun of consumed recovery jobs after failure. Preserve checkpoints and stop on missing/untrusted receipts. Missing credentials/data/resources or evidence drift is a specific blocker, not permission to fetch or invent facts.

Validation: meaningful tests for untouched failed parent, derived-path isolation, global94/98 carry-forward, no fit even on expansion, batch-sensitive replay, old-score corruption, missing-label members, changed row order, source/runtime tamper, locks and consumed recovery jobs. Complete full uv run pytest, Ruff/diff and the optional runtime suite if modified. Independently audit all314804derived rows, originals unchanged and combined100/104 coverage. Deliver Chinese read-only UI/report showing original failed status AND separate recovery outcome; downloads must distinguish those identities. Keep the full-result presentation drafts inactive. Commit/push each reviewable part, exact green PR CI and full handoff; no master merge until annual Issue84 is truly complete.

After this ZERO-FIT recovery is independently closed, issue one next bounded card for explicit carry-forward execution of ONLY the four unused originally declared new-fit slots under the single cumulative98 ceiling, then the full104-reference/968daily/208weekly/48monthly/80paired independent review. The next card must define durable reservations and failure semantics before any of those fits; this card does NOT authorize them. Only after that complete annual delivery should the existing fixed economic-comparison draft be activated. The user goal remains strategy prediction and economic value first, Alpha101/191 and complex risk overlays later.
