# Fixed S4-A first-entry plan and necessary-fact bridge

Direct approved v3 commission. Exact clean pushed expected_head:
3e614c3cf19bff1922c671e0bbdafe9d87a3e11a; both exact master CI34736755422 jobs passed.
No concurrent writer; heavy lock free; old shared loop remains paused.
Push this card before implementation and before inspecting newly ranked results.

Connect the already implemented quantity scheduler inputs to a concrete first
S4-A entry intention, so required historical evidence can be scoped to actually
proposed holdings rather than all256 securities or every corporate event.
This is a prerequisite/intent projection, not a simulated path or recommendation.
No new provider calls, formulas, candidates, model fits, economic paths, orders,
canonical/account writes, actual future labels, promotion or old-run repetitions.

Freeze first decision2021-12-31, intended execution2022-01-04, following2022-01-05.
Retain the original sorted256 cohort and copy the already saved S4-A score only.
At least30 finite scores are necessary; choose the highest20, descending score
and ascending identifier for exact ties. This is a raw signal proposal: historical
ST/lifecycle eligibility is not implied. Unknown score rows stay explicitly in
the full population; no order-day bar, status, liquidity or corporate field may
change this selection or backfill a failed slot. No downstream NAV is calculated.

Initial cashCNY200000, initially flat, gross target80%,20 equal nominal slots:
CNY8000 per slot, unallocated/blocked slots remain cash without redistribution.
Use only the decision day's valid unadjusted close to compute desired nominal
shares, then the separately reviewed closing-LIMIT basic buy grid; do not size
from execution-day close or adjusted prices. Floor to valid min+increment grid,
respect single-order max without splitting. Zero-below-minimum is explicitly
unaffordable, not an unknown observation; unknown decision price/rule is unknown.
The nominal quantity is not an affordable executable order: fee-inclusive cash
and actual capacity are handled by the existing kernel only after facts qualify.
The slot allowance is not an order price or a broker reservation.

Reuse original stock_replay_inputs observations/report/proof and lower-bound
liquidity report/independent_proof/projection, original selection/calendar, original
2021-12-31 daily partition, user cost profile and closing catalogue source chain.
Before actual: bind exact paths, fingerprints, hashes and bytes in a new config.
Read only these saved inputs, never recompute old signal or ADV writers. Attach
explicitly named prior20_amount_lower_bound_fen to new projected contexts; keep
old observations and169 strict ADV values untouched. Raw execution amount, volume
and prices keep their original strict conversion and may remain unknown.

Freeze two future fee contexts, baseline adverse slippage5bp and stress15bp;
both use the already approved conservative priorADV20 participation1%. The5%
capacity sensitivity remains a later budgeted experiment, not another hidden path.
User-declared commission0.000086/min500fen, buy stamp0, sell stamp0.001 before
2023-08-28 and0.0005 thereafter are carried as declared/reviewed components.
Additional rate/fixed fees stay None, never zero; no complete-cost authority.
No sampled spread claim or fee-cost reclassification. Do not start either economic
path while necessary evidence remains unknown; later attempts count toward the
existing S4-A two reserved slots. No repeatable uncounted failed simulation.

Report every necessary missing fact for each selected positive-quantity proposal,
not merely the first global blocker: market state, corporate processing, raw
price/limit/amount/volume, prior20 coverage, quantity rule and complete fees.
Absent ST/suspension rows are not normal-trading proof. Original exception rows
remain observations; do not infer new boolean status from them. Keep the whole
first-day batch stopped before any hypothetical mutation when critical facts are
unknown, with an initial-flat/no-complete-day statement rather than zero return.
Do not run the scheduler on real inputs under this card; attach validated contexts
and proposed orders only for future consumption. Economic outcome fields stay None.

One actual projection <=300s,2GiBRSS,64MiB output,8GiB physicalD reserve, one heavy
lock; one separately implemented scalar proof verifies ranks/grid/values/reasons.
One source-config preparation, no source downloads. Failed starts remain sealed.
Tests: selection unaffected by execution data, ties/unknowns/min30, full256 retained,
raw-vs-adjusted sizing, main/STAR grids, nominal gross/cash conservation, no backfill,
strict date/source fingerprints, preserved unknown fees/status/corporate inputs,
all-blocker disclosure and immutable originals. Full pytest/Ruff/diff, self-review,
commit/push before actual/proof, PRCI/merge/exact-masterCI and concise findings.

After this bridge freeze the exact required unknowns once. Use that bounded list
for a later new source-qualification or independent approved strategy card. Do not
rerun this projection across heartbeats or expand unrelated corporate audits.
