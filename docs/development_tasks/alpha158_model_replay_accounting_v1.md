# Commission continuation: actual-price scenario accounting

Expected HEAD b9a653b30468740b77e6ce67969cb6fadb84d485. User explicitly requests continued implementation and the previously commissioned backtest. Single agent; Linux worktree clean, old automation paused.

Preserve saved ranking, daily refresh, 20 slots, initial CNY200000 and fee assumptions. Implement a separate orchestrator around the unchanged quantity kernel; do not relax the frozen scheduler raw-mark contract. Size next-session intents from decision-date equity and marks, sells before buys, buys in fixed rank order. No leverage. Fixed participation cap 5% of both session volume and the lesser of session turnover and prior20 mean turnover. Prior20 means 20 market sessions; verified suspended dates contribute observed zero activity, unexplained absent bars stop. This explicit capacity definition belongs to this new replay, not an amendment of S4.

Known suspension blocks all execution. Valuation uses the last observed raw close with its actual observation date and an explicit stale-valuation flag; it is never supplied as a current execution quote. Stop if corporate actions would make a stale quote invalid or delisting/code-change accounting is unresolved. Current master listing dates plus pinned historical code-change mapping constrain identities; daily vendor records remain retrospective evidence, not historical receipt-time certification.

Use implemented dividend terms from hash-verified archived responses for accounting only, not signals. Capture actual record-date lots after trading. On ex-date create cash receivables, release on pay date; record taxation on FIFO disposal under published calendar-month/year rules and reserve outstanding modeled tax in NAV. Share distributions require known dates, recipient quantities and taxable breakdown; unresolved data stop the replay for targeted evidence repair, not deletion of stocks or fabricated cash. Preserve every attempt and failed prefix in a fresh output directory. Additional provider acquisition remains within the user's selected-stock dividend/delist authorization.

Full-period metrics only after a complete path. Distinguish simulated executable-price constraints from broker fills; disclose stale valuations, remaining positions and data limitations. No new model/strategy/risk optimisation, training, canonical writes, live orders or automated schedules. Test conservation, ex/pay separation, FIFO tax, sellability and suspension handling, then full pytest/ruff/diff.

## Direct user amendment before complete outcomes

User chooses zero valuation from delisting, retaining unknown recovery rights and share quantities without any synthetic sale/cash. User also commissions an ex-ST comparison. Run exactly two variants: original all finite scores and exclude ST using each signal day's canonical stock_st records BEFORE descending score ranking and refill to at most20. Same model scores, costs, dates, sizing and execution rules. This is a retrospective portfolio-filter comparison, not new training or independent fresh out-of-sample evidence. Both variants use zero delisted valuation; do not optimize further variants against the results.
